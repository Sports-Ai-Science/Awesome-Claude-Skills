# Claude Code ガイドライン - Scrum + TDD + Quality Gate

このドキュメントは、Claude Codeがこのプロジェクトで効果的に開発を行うためのガイドラインです。

## 開発フロー概要

```
ユーザーリクエスト
       ↓
┌──────────────────────────────────────────────────────────┐
│ 1. 要件分析・タスク分解（スクラム思考）                  │
│    - ユーザーストーリーの理解                            │
│    - Acceptance Criteriaの確認                          │
│    - タスクをTodoリストに分解                            │
└──────────────────────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────────────────────┐
│ 2. TDDサイクル実行（各タスクごと）                       │
│    RED   → テストを先に書く                             │
│    GREEN → 最小限の実装                                 │
│    REFACTOR → リファクタリング                          │
└──────────────────────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────────────────────┐
│ 3. Quality Gate チェック                                │
│    - テスト実行・カバレッジ確認                          │
│    - リント・フォーマットチェック                        │
│    - セキュリティスキャン                                │
└──────────────────────────────────────────────────────────┘
       ↓
   コミット・プッシュ
```

## 必須ルール

### 1. タスク管理（スクラム）

```
【必須】TodoWriteツールを使用してタスク管理を行う

例：
- ユーザーストーリーを受け取ったら、タスクに分解
- 各タスクの状態を常に更新（pending → in_progress → completed）
- 1つずつ完了させてから次へ進む
```

### 2. TDD（テスト駆動開発）

```
【必須】実装前にテストを書く

順序：
1. RED: 失敗するテストを書く
2. GREEN: テストを通す最小限の実装を書く
3. REFACTOR: コードを改善する（テストは通ったまま）
```

**TDD実践例:**

```python
# Step 1: RED - 失敗するテストを書く
def test_add_numbers():
    assert add(2, 3) == 5  # add関数はまだ存在しない

# Step 2: GREEN - テストを通す最小限の実装
def add(a, b):
    return a + b

# Step 3: REFACTOR - 必要に応じて改善
def add(a: int, b: int) -> int:
    """2つの数値を加算する"""
    return a + b
```

### 3. Quality Gate

コミット前に以下を確認：

| チェック項目 | コマンド例 | 基準 |
|-------------|-----------|------|
| テスト実行 | `pytest` / `npm test` | 全てパス |
| カバレッジ | `pytest --cov` | ≥ 80% |
| リント | `ruff check` / `eslint` | エラー 0 |
| フォーマット | `ruff format --check` | 差分なし |
| 型チェック | `mypy` / `tsc` | エラー 0 |

## 開発フロー詳細

### Phase 1: 要件分析

```markdown
ユーザーリクエストを受け取ったら：

1. 要件を理解し、不明点があれば質問する
2. Acceptance Criteria（受け入れ条件）を明確化する
3. タスクを分解してTodoリストに登録する

例）「ユーザー登録機能を追加して」
   → タスク分解：
   - [ ] ユーザーモデルの設計
   - [ ] バリデーションのテスト作成
   - [ ] ユーザーモデルの実装
   - [ ] 登録APIのテスト作成
   - [ ] 登録APIの実装
   - [ ] E2Eテストの作成
```

### Phase 2: TDDサイクル

各タスクに対して：

```
┌─────────────────────────────────────────────────────────┐
│                    TDD CYCLE                            │
│                                                         │
│    ┌─────────┐                                         │
│    │   RED   │ ← テストを書く（失敗する）              │
│    └────┬────┘                                         │
│         ↓                                               │
│    ┌─────────┐                                         │
│    │  GREEN  │ ← 最小限の実装（テストが通る）          │
│    └────┬────┘                                         │
│         ↓                                               │
│    ┌──────────┐                                        │
│    │ REFACTOR │ ← リファクタリング（テストは通る）     │
│    └────┬─────┘                                        │
│         ↓                                               │
│    テストが全て通ることを確認                          │
│         ↓                                               │
│    次の機能へ or コミット                               │
└─────────────────────────────────────────────────────────┘
```

### Phase 3: Quality Gate

```bash
# Python プロジェクトの場合
pytest --cov --cov-report=term-missing  # テスト＆カバレッジ
ruff check .                             # リント
ruff format --check .                    # フォーマット
mypy .                                   # 型チェック

# JavaScript/TypeScript プロジェクトの場合
npm test -- --coverage                   # テスト＆カバレッジ
npm run lint                             # リント
npm run format:check                     # フォーマット
npm run typecheck                        # 型チェック
```

**Quality Gateが失敗した場合:**
1. 失敗した項目を特定
2. 修正を行う
3. 再度Quality Gateを実行
4. 全てパスするまで繰り返す

### Phase 4: コミット

```bash
# Quality Gate通過後にコミット
git add <files>
git commit -m "feat: ユーザー登録機能を追加

- ユーザーモデルとバリデーション実装
- 登録APIエンドポイント追加
- テストカバレッジ 85%

Quality Gate: PASS"
```

## チェックリスト

開発時に常に確認：

- [ ] タスクがTodoリストに登録されているか
- [ ] テストを先に書いたか（TDD）
- [ ] テストが全てパスしているか
- [ ] カバレッジが80%以上か
- [ ] リント・フォーマットエラーがないか
- [ ] 型エラーがないか
- [ ] コミットメッセージが明確か

## 禁止事項

1. **テストなしの実装** - 必ずテストを先に書く
2. **Quality Gate未通過のコミット** - 全てのチェックをパスしてからコミット
3. **大きすぎるコミット** - 1つの機能・修正につき1コミット
4. **セキュリティ脆弱性の放置** - 発見次第即座に対応

## エラー対応

### テストが失敗する場合
```
1. エラーメッセージを確認
2. 期待値と実際の値を比較
3. 実装を修正
4. テストを再実行
```

### Quality Gateが通らない場合
```
1. どのゲートで失敗しているか確認
2. 該当する修正を実施
3. 全ゲートを再実行
4. 全てパスするまで繰り返す
```

## 関連ドキュメント

- [スクラムワークフロー](docs/scrum/README.md)
- [TDDガイド](docs/tdd/README.md)
- [Quality Gate基準](docs/quality-gate/README.md)
